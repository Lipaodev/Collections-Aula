package me.felipe

 fun main () {
    val values =  intArrayOf (12,2,4,8,10,12) //Opção de dimensionamento do array, podendo ser alterado mesmo sendo constante.


        values.forEach {
            println(it)
        }


           println(  )

            values.sort()
            values.forEach {
                println(it)
            }

 }
